<view class="container">
  <view class="search-bar">
    <input class="search-input" placeholder="请输入设备名称" bindinput="onSearchInput" />
    <button class="search-btn" bindtap="search">搜索</button>
  </view>
  
  <view class="equipment-list">
    <view wx:for="{{equipmentList}}" wx:key="id" class="equipment-item" bindtap="goToDetail" data-id="{{item.id}}">
      <view class="equipment-info">
        <view class="equipment-header">
          <text class="equipment-name">{{item.name}}</text>
        </view>
        <text style="color: #c3c3c3;">设备状态：</text>
        <text class="equipment-status status-badge"style="background-color: {{item.statusBg}};color:white;">{{item.statusName || ''}}</text>
      </view>
      <view class="arrow-icon">
        <view style="color: {{item.approvalStatusId === 1 ? '#ff4d4f' : item.approvalStatusId === 2?'#52c41a':'#1890ff'}}">{{item.approvalStatusName || '待审核'}}</view>
        <text class="arrow">›</text>
      </view>
    </view>
  </view>
  
  <view wx:if="{{isLoading}}" class="loading">
    <text>加载中...</text>
  </view>
  
  <view wx:if="{{!isLoading && equipmentList.length === 0}}" class="empty">
    <text>暂无设备数据</text>
  </view>
</view>
